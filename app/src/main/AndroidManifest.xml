<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PopMovies">

        <activity android:name=".ui.activity.ChatActivity"/>
        <activity android:name=".ui.activity.ReviewsActivity" />
        <activity android:name=".ui.activity.ViewPictureActivity" />
        <activity android:name=".ui.activity.FavouritesActivity" />
        <activity android:name=".ui.activity.ForgotPasswordActivity" />
        <activity android:name=".ui.activity.AccountVerificationActivity" />
        <activity android:name=".ui.activity.SignUpActivity" />
        <activity
            android:name=".ui.activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.ProfileActivity" />
        <activity android:name=".ui.activity.MovieActivity" />
        <activity
            android:name=".ui.activity.MovieDetailsActivity"
            android:theme="@style/Theme.PopMovies.Details" />
    </application>
</manifest>